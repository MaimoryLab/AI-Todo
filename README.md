# AI-Todo

Clean rebuild workspace for `@maimorylab/ai-todo`.

Current target:

- Local-first todo extraction for AI agent sessions.
- Clean `ai-todo` runtime with CLI, HTTP, and MCP entry points.
- Deliberate migration of useful parser/scanner/extraction behavior without importing the old repository wholesale.
